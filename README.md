# project-pillars-roof
Project-Pillars-Roof is a framework that dual-boots devices using their factory stock OS or LineageOS fallback. It adds Debian Linux as a second boot option running directly on top of a heavily modded Android base layer for native hardware access.

